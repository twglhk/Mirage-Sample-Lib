using Mirage;
using Mirage.Logging;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetworkSpawnableSub : NetworkSpawnable
{
    static readonly ILogger logger = LogFactory.GetLogger(typeof(NetworkSpawnableSub));
}
